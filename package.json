{
  "name": "netflix-display-helper",
  "version": "1.3.2",
  "private": true,
  "description": "A browser-only compositor fix for black Netflix video on DisplayLink monitors.",
  "scripts": {
    "check": "node --check background.js && node --check compositor-fallback.js && node --check content.js && node --check display-utils.js && node --check popup.js",
    "test": "node --test"
  }
}
